name: Astro CI

on:
  push:
    branches: main
  pull_request:
    branches: main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

#      - name: Install
#        run: yarn install

#      - name: Build
#        run: yarn build

      - name: Test
        run: ls -lah ./docs && cat ./docs/index.html
